A RUN THROUGH OF THE PARSING ALGORITHM

> first through readline it reads the user input (which would be a char * string)
> then we will call the check_for_syntax_error function to check if there's a syntax error
 such as consecutive pipes or empty pipes or redirection
> onced passed we will use a modified ft_split to turn this string into a multi-array command or values ready to be tokenized
> then we will traverse through it and turn them into a token
> once made into token we will turn them into an ast

> ast explanation